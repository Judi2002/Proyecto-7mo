Librerias 
definicion numeros pines arduino
void setup() {  
definicion pines funcionales arduino
printf ingrese un balde 
}

void loop() {

  /*if el disco esta en punto neutro( 
   motor discor = 0
    )else(
 */    
  if recipiente = 1[
  digitalwrite recipiente=1
  /*posicionamiento de disco en neutro mediante whiles, calculos de medida y parar el motor con rle*/
  ingrese el el valor mediante el teclado, acá se pone como toman los botones
  if canister1 = 1(
    while Llenocanister = 0(
      printf llene el canister 1
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
      ))

      
  if canister2 = 1(
    while Llenocanister = 0(
      printf llene el canister 2
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
      ))

      
  if canister3 = 1(
    while Llenocanister = 0(
      printf llene el canister 3
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
      ))

      
  if canister4 = 1(
    while Llenocanister = 0(
      printf llene el canister 3
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
      ))

      
  if canister5 = 1(
    while Llenocanister = 0(
      printf llene el canister 3
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
      ))

      
   if los 5 canisters estan en 0( 
    interrupcionR
   )else(
     printf ¿agrego la tapa?
     while btnsi = 0(
      printf agregue tapa
      )else(
        motor mezcladora = 1
        delay (ej 1000)
        motor mezcladora = 0
        delay(ej 500)
      )

         
     while btnsi y btno sean 0(
      printf ¿desea que el color sea mas claro?  /*es para que espere a la confirmación*/
      )else(
        if btnsi = 1 ( 
          while Llenocanister = 0(
      printf llene el canister 3
      )else(
        motordisco = 1, hasta que se posicione
        valvcanister = 1
        delay (el tiempo que necesite, ej 500)/*asi funciona como elecetrvalvula*/
        valvcanister = 0 
        while(ej 500)
        motor mezcladora = 1
        delay (ej 1000)
        motor mezcladora = 0
        delay(ej 500)
        btnsi = 0
      ))else(
        if btnno = 1 (
          
/* hay que terminar el final, hay que aclacar como vamos a hacerlo, si con while o do while, tuve una idea de usar estados con los botones de 
*  confirmacion y negcaion, pero tambien me gusta mas la idea de usar dos botones independientes para enclarecer u oscurecer el color final
*  con su respectiva confirmacion. Tambien hace flata preguntar si se queire mezlñar mas, depsues terminw uwu uwu uwu
*/
     
     
     ]else printf ingrese balde
}
interrupcionR(
  printf ERROR
  reiniciar programa
  )
